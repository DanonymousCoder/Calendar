let userAllowed;
let age = prompt('How old are you?', ' ');

if (age > 5) {
    userAllowed = true;
    alert(`Acces Allowed status: ${userAllowed}`)
}
else if (age)
else {
    userAllowed= false;
    alert(`Acces Allowed status: ${userAllowed}`)
}